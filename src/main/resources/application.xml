<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

    <context:component-scan base-package="me.frenchline.springapplicationcontext"/>
    <!-- context의 component-scan 이용한 Spring Bean 등록 방법 -->
    <!-- 1. base-package: 이 패키지를 포함해 하위 패키지에서 Bean을 스캐닝(애노테이션을 스캐닝)해서 등록 -->
    <!-- 2. 기본적으로 컴포넌트 애노테이션(@Component)을 사용해서 Bean을 등록할 수 있다 -->
    <!-- 3. 또한 @Component를 확장받은 애노테이션들을 사용해 Bean으로 등록할 수 있다 -->
    <!-- Ex) 메타 애노테이션 @Component를 확장받은 컴포지트 애노테이션: @Service, @Repository, @Controller, @Configuration -->

</beans>